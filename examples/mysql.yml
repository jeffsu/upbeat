services:
  mysql:
    actions:
      mysql:
        strategy: mysql # custom query
        sql: "SELECT * FROM USERS"
        host: localhost
      mysql-default:
        strategy: mysql 
        host: localhost
