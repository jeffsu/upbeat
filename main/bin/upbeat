#!/usr/bin/env node

require('mochiscript');

var assembler = require('../lib/assembler');

if (process.argv.length !== 3) {
  console.log("USAGE: upbeat <configFile>");
  process.exit(0);
}

var configFile = process.argv[2];
var server = assembler.assemble(configFile);
server.run();
